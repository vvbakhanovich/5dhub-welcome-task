spring:
  datasource:
    url: jdbc:h2:mem:companies-db
    username: root
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
server:
  port: 8070
application:
  user-service-url: http://${USER_SERVICE_HOST:localhost}:8282/api/v1/users